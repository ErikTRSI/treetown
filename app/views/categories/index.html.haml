%h1 Edit Categories
%ul
- @categories.each do |c|
  %li
    = link_to c.name, edit_category_path(c)
    %ul
      -c.pages.each do |p|
        %li
          =link_to p.title, edit_category_page_path(c, p)
